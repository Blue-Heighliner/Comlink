<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
    <RootNamespace>BlueHeighliner.Comlink</RootNamespace>
    <AssemblyName>BlueHeighliner.Comlink</AssemblyName>
    <AvaloniaUseCompiledBindingsByDefault>false</AvaloniaUseCompiledBindingsByDefault>
    <NoWarn>AVLN3001</NoWarn>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)' == 'Release'">
    <!--
      Embedded (not portable) debug symbols: Sample publishes as a single-file self-contained
      deployment (PublishSingleFile) that references Core, and a separate .pdb would remain a
      loose file outside that bundle. Embedding keeps the whole deployment to one file. This is
      also why IncludeSymbols/SymbolPackageFormat=snupkg are deliberately left off the PackageId
      property group below - a snupkg built against an embedded-symbol DLL has no .pdb to include.
    -->
    <DebugType>embedded</DebugType>
  </PropertyGroup>

  <PropertyGroup>
    <PackageId>BlueHeighliner.Comlink</PackageId>
    <Authors>Chris Markwardt</Authors>
    <Description>A peer-to-peer messaging engine that connects nodes over IP with the Mercury Secure Message Transport (MSMT) protocol or point-to-point over MicroGate serial cables, with a built-in Avalonia desktop GUI. Avalonia is a required dependency of this package, not optional - it can be run headless (no window shown), but the GUI layer and its dependencies are always present.</Description>
    <PackageTags>comlink;messaging;peer-to-peer;msmt;microgate;serial;avalonia</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryUrl>https://github.com/Blue-Heighliner/Comlink</RepositoryUrl>
    <PackageProjectUrl>https://github.com/Blue-Heighliner/Comlink</PackageProjectUrl>
    <GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <AvaloniaResource Include="Assets/Fonts/DejaVuSansMono.ttf" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="BlueHeighliner.MicroGate" Version="0.1.0" />
    <PackageReference Include="BlueHeighliner.Msmt" Version="0.1.5" />
    <PackageReference Include="protobuf-net" Version="3.2.30" />
    <PackageReference Include="Avalonia" Version="11.3.0" />
    <PackageReference Include="Avalonia.AvaloniaEdit" Version="11.3.0" />
    <PackageReference Include="Tmds.DBus.Protocol" Version="0.21.3" />
    <PackageReference Include="Avalonia.Desktop" Version="11.3.0" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="11.3.0" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.0" />
    <PackageReference Include="LiteDB" Version="5.0.21" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <None Include="../README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
